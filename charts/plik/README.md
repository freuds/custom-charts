# plik

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.4.2](https://img.shields.io/badge/AppVersion-1.4.2-informational?style=flat-square)

Ephemeral file sharing service (root-gg/plik) with an S3-compatible object storage backend

**Homepage:** <https://github.com/root-gg/plik>

## Source Code

* <https://github.com/root-gg/plik>

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` |  |
| commonLabels | object | `{}` | Additional labels to add to all resources created by this chart |
| config.content | string | `"Debug               = false\nDebugRequests       = false\nLogLevel            = \"INFO\"\n\nListenPort          = 8080\nListenAddress       = \"0.0.0.0\"\nMetricsPort         = 0\nMetricsAddress      = \"0.0.0.0\"\n\nDataBackend = \"file\"\n[DataBackendConfig]\n    Directory = \"/data/files\"\n\n[MetadataBackendConfig]\n    Driver = \"sqlite3\"\n    ConnectionString = \"/data/plik.db\"\n    Debug = false\n"` | Rendered into a ConfigMap and used only when existingSecret is unset. Fine for backends with no credentials to embed (e.g. the default "file" backend below); switch to existingSecret for S3. |
| config.existingSecret | string | `""` | Name of an existing Secret providing plikd.cfg (key: existingSecretKey below). Takes priority over `content`. Use this whenever the config carries credentials -- ConfigMaps are not encrypted at rest, Secrets at least get RBAC-scoped access. |
| config.existingSecretKey | string | `"plikd.cfg"` |  |
| extraEnv | list | `[]` | Extra environment variables on the plik container. Plik reads config overrides from PLIKD_<SCREAMING_SNAKE_FIELD_NAME> env vars for its top-level scalar settings (e.g. PLIKD_ABUSE_CONTACT) -- this does NOT reach into DataBackendConfig/MetadataBackendConfig, which are untyped maps only settable from plikd.cfg itself (see config.existingSecret below). |
| fullnameOverride | string | `""` |  |
| image.pullPolicy | string | `"IfNotPresent"` |  |
| image.repository | string | `"rootgg/plik"` |  |
| image.tag | string | `""` | Overrides the image tag whose default is the chart appVersion |
| imagePullSecrets | list | `[]` |  |
| ingress | object | `{"annotations":{},"className":"","enabled":false,"hosts":[{"host":"chart-example.local","paths":[{"path":"/","pathType":"ImplementationSpecific"}]}],"tls":[]}` | Generic Ingress, kept for portability of this chart outside its origin cluster. Clusters that front services with something other than a plain Ingress controller (e.g. Traefik IngressRoute) should leave this disabled and add their own routing resource alongside the release instead. |
| livenessProbe.httpGet.path | string | `"/"` |  |
| livenessProbe.httpGet.port | string | `"http"` |  |
| metrics | object | `{"enabled":false,"port":9200}` | Requires MetricsPort to be set to a non-zero port matching `metrics.port` in the plikd.cfg supplied via `config` above -- this chart only wires up the container/Service port and (optionally) the ServiceMonitor, it can't see inside an externalSecret-sourced config to confirm they match. |
| nameOverride | string | `""` |  |
| nodeSelector | object | `{}` |  |
| persistence | object | `{"accessMode":"ReadWriteOnce","enabled":true,"existingClaim":"","mountPath":"/data","size":"1Gi","storageClassName":""}` | Holds the sqlite metadata db (upload records/TTLs) and, when using the "file" DataBackend above, the uploaded files themselves. |
| podAnnotations | object | `{}` |  |
| podLabels | object | `{}` |  |
| podSecurityContext | object | `{}` |  |
| readinessProbe.httpGet.path | string | `"/"` |  |
| readinessProbe.httpGet.port | string | `"http"` |  |
| replicaCount | int | `1` |  |
| resources | object | `{}` |  |
| securityContext | object | `{}` |  |
| service.port | int | `8080` |  |
| service.type | string | `"ClusterIP"` |  |
| serviceAccount.annotations | object | `{}` |  |
| serviceAccount.create | bool | `true` | Specifies whether a ServiceAccount should be created |
| serviceAccount.name | string | `""` | The name of the ServiceAccount to use. If not set and create is true, a name is generated using the fullname template |
| serviceMonitor.enabled | bool | `false` | Requires the Prometheus Operator CRDs and metrics.enabled above |
| serviceMonitor.interval | string | `"1m"` |  |
| serviceMonitor.labels | object | `{}` |  |
| serviceMonitor.namespace | string | `""` |  |
| serviceMonitor.path | string | `"/metrics"` |  |
| serviceMonitor.scheme | string | `"http"` |  |
| tolerations | list | `[]` |  |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
