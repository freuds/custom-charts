apiVersion: v1
kind: Service
metadata:
  name: {{ include "ihm.fullname" . }}
  labels:
    {{- include "ihm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ihm.selectorLabels" . | nindent 4 }}
